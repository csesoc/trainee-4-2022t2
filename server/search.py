import re

manpages = ["codeordie"]

def search(input):

for pages in manpages:
    if (input === pages) {
        return page;
    }
    for page in manpages 
        matches = re.findall(input, pages)


